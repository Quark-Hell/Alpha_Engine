#Engine source
SET (ALPHA_Engine "${PROJECT_SOURCE_DIR}")
SET (Engine_Source "${ALPHA_Engine}/Engine")
SET (Engine_Assets "${ALPHA_Engine}/Assets")

SET (Example "${ALPHA_Engine}/Build_Files")
SET (Examples_Build_Dir "${CMAKE_BINARY_DIR}/Build")

SET (Tests "${ALPHA_Engine}/Tests")
SET (Tests_Build_Dir "${CMAKE_BINARY_DIR}/Tests")

include_directories(${Engine_Source})

#Engine libraries
set (GLFW "${ALPHA_Engine}/external/GLFW")
include_directories("${GLFW}/include")

set (GLM "${Engine_Source}/Core/External/GLM")
include_directories("${GLM}")

set (GLAD "${ALPHA_Engine}/external/GLAD")
include_directories("${GLAD}/glad/include")

set (assimp "${ALPHA_Engine}/external/assimp")
include_directories("${assimp}/include")

set(STB "${ALPHA_Engine}/external/STB")
include_directories(${STB})

set(bitsery "${ALPHA_Engine}/external/bitsery")
include_directories(${bitsery})
#Engine libraries

set(Build_Dir "${CMAKE_BINARY_DIR}/Build" CACHE PATH "Custom build dir")
set(Configs_Dir "${Build_Dir}/Configs")
set(LIBRARY_OUTPUT_PATH "${CMAKE_BINARY_DIR}/Build/lib")

#Game source
set (Game "${BuildDir}")
set (Game_Files_Dest "${Game}/Game")
set (Game_Engine_Dest "${Game_Files_Dest}/ALPHA_Engine")
set (Game_Source_Dest "${Game_Files_Dest}/Source")

#Utility
set(SCAN_DIRS
    "${ALPHA_Engine}/Engine/Core/"
)